import './footer.css';

export default function Footer() {
  return (
    <footer className="footer">
      <span className="footer-unit">UNIT SYSTEM: METRIC (cm, kN, kg/s, K, MPa)</span>
      <span className="footer-version">Version - 0.1</span>
    </footer>
  );
}
