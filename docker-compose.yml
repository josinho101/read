services:
  app:
    image: localhost/read-app:0.1.4
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - api/.env
    volumes:
      - ./api/saved_engines:/app/saved_engines
      - ./api/secrets:/app/secrets:ro
